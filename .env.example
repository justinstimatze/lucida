# Copy to .env and fill in. .env is gitignored.

ANTHROPIC_API_KEY=sk-ant-...

# Only needed if using the image substrate (--type image).
# Install image extras first: uv pip install -e .[image]
# GOOGLE_API_KEY=AIza...
